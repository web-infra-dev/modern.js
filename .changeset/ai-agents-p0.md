---
'@modern-js/create': patch
---

feat(create): scaffold a lightweight AGENTS.md / CLAUDE.md by default, with `--no-agents-md` and `--skills=none|recommended|custom` flags (Skills are suggested, never auto-installed)

feat(create): 创建项目时默认生成轻量 AGENTS.md / CLAUDE.md，新增 `--no-agents-md` 与 `--skills=none|recommended|custom` 参数（Skills 仅建议、不自动安装）
