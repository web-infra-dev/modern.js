---
"@modern-js/server-core": patch
---

Synchronize Node module exports with validated selectively reloaded SSR entries, releasing stale request handlers while preserving unrelated entry state.

同步局部更新后的 SSR 入口导出与 Node 模块缓存，释放旧请求处理函数并保留未受影响入口的状态。
