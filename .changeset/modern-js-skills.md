---
'@modern-js/skills': minor
---

feat(skills): add @modern-js/skills — a zero-dep CLI to install official Modern.js Agent Skills (list / add --target=claude|codex|cursor|all) into a project's agent directories. Bundles `modernjs-dependency-audit` and `modernjs-migrate-to-v3`.
