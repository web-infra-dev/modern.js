---
'@modern-js/app-tools': minor
---

Export the programmatic `build` API and its options from the package root, and initialize the builder when `build` is supplied through the app-context command.

从包根导出程序化 `build` API 及其选项，并在通过 app-context command 传入 `build` 时初始化 builder。
