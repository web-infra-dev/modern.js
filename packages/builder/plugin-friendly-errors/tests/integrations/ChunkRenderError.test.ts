import { test } from '@modern-js/e2e/playwright';
import { createStubBuilder } from '@modern-js/builder-webpack-provider/stub';

// import { webpackPromise } from '../utils';

test('ChunkRenderError', async () => {
  // const options = useFixture();
  createStubBuilder();
});
