import { BrowserRouter } from 'react-router-dom';
import { Layout } from '../theme-default/Layout';

export function App() {
  return (
    <BrowserRouter>
      <Layout />
    </BrowserRouter>
  );
}
