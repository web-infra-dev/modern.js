export type LegacyUserConfig = {
  designSystem?: Record<string, any> & {
    supportStyledComponents?: boolean;
  };
  source?: {
    designSystem?: Record<string, any> & {
      supportStyledComponents?: boolean;
    };
  };
};
