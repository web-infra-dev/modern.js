import { Box, Text } from '@radix-ui/themes';
import type React from 'react';

const Page: React.FC = () => {
  return (
    <Box>
      <Text>React DevTools is unavailable for offscreen.</Text>
    </Box>
  );
};

export default Page;
