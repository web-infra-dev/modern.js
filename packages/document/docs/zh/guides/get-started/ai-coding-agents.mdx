---
title: AI 工具
sidebar_position: 6
---

# Modern.js For AI

Modern.js 为 AI Agent 提供了一套工具套件，帮助开发者利用 AI 高效完成功能开发、依赖升级与版本迁移。它分三层：优先用**随包文档 + AGENTS.md**（默认、离线、与安装版本匹配），覆盖不到时用 **llms.txt** 在线兜底，多步骤流程交给 **Skills**。

## 随包文档与 AGENTS.md

这是 AI Agent 获取 Modern.js 知识的**首选来源**。新建的 Modern.js 项目开箱即为 agent-ready：

- **随包文档**：`@modern-js/app-tools` 内置了与安装版本一致的英文文档，位于 `node_modules/@modern-js/app-tools/docs/`，离线可用。
- **AGENTS.md / CLAUDE.md**：`npx @modern-js/create` 自动生成，引导 AI 编码助手在写代码前先读随包文档，而不是依赖过时的训练数据。

Modern.js 只维护 `AGENTS.md` 中 `<!-- BEGIN:modernjs-agent-rules -->` 标记内的内容，标记之外你自己写的内容不会被改动。不需要时加 `--no-agents-md` 可跳过生成。

### 已有项目

升级 `@modern-js/app-tools` 后，随包文档会自动出现在 `node_modules` 中。在项目根运行以下命令，即可生成或更新 `AGENTS.md` / `CLAUDE.md`：

```bash
npx @modern-js/create agents-md
```

可随时重复运行以同步到最新版本，你的自定义内容会被保留。

## llms.txt（在线兜底）

随包文档只包含你**当前安装的版本**，并为控制体积裁掉了少数章节（社区、教程、插件开发）。遇到随包文档覆盖不到的场景——查阅其他版本、被裁掉的章节，或对照最新的在线文档——再用 llms.txt。

Modern.js 文档遵循 [llms.txt 规范](https://llmstxt.org/)，由 [`@rspress/plugin-llms`](https://rspress.rs/plugin/official-plugins/llms) 自动生成：

- 索引：[`https://modernjs.dev/llms.txt`](https://modernjs.dev/llms.txt)
- 全文：`https://modernjs.dev/llms-full.txt`（体积较大，按需取片段）

让 Agent 按需在线检索即可，不必把文档复制进项目。

## Skills

Skills 是按需触发的 AI 辅助能力，遵循 [Agent Skills 开放标准](https://github.com/vercel-labs/skills)。用户向 Skills：

| Skill | 标识 | 说明 |
|---|---|---|
| 升级到 v3 | `modernjs-migrate-to-v3` | v2 应用迁移到 v3：安全改写 + 复杂项人工清单 + 迁移报告 |
| 启用特性 | `modernjs-feature-enable` | 为 v3 应用启用 BFF / SSG / styled-components，并脚手架化 Tailwind CSS / 自定义 Web Server |

> Skills 默认不强装、不隐式安装，由你显式安装。RSC、微前端等属于配置或架构决策，不是 `modernjs-feature-enable` 的一键启用项。

## 安装 Skills

Modern.js 的用户向 Skill 就放在仓库根 `skills/` 目录，遵循 [Agent Skills 开放标准](https://github.com/vercel-labs/skills)。推荐用标准的 `skills` CLI 直接从 GitHub 安装：

```bash
# 列出可安装的 Skills
npx skills add web-infra-dev/modern.js --list

# 安装单个 Skill 到你的 Agent 目录（--agent 可选 claude-code / codex / cursor 等）
npx skills add web-infra-dev/modern.js --skill modernjs-migrate-to-v3 --agent codex -y
```

它会把整个 Skill 目录（`SKILL.md` + `scripts/` + `references/`）安装到对应 Agent 目录，随后即可在该 Agent 里触发。

> 需锁定到某个版本时，在仓库后加 `#<ref>`（ref 可为 tag / 分支 / commit），即安装该版本上的 Skill（把 `<tag>` 换成含本 Skill 的发布 tag）：
>
> ```bash
> npx skills add web-infra-dev/modern.js#<tag> --skill modernjs-migrate-to-v3 --agent codex -y
> ```
