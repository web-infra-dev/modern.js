---
sidebar_position: 4
---

# 路径别名

import Alias from '@modern-js/builder-doc/docs/zh/shared/alias';

<Alias />

## 默认别名

Modern.js 框架内置了以下别名:

```json
{
  "@": "./src",
  "@shared": "./shared"
}
```

此外，在启用框架的 BFF 插件时，默认会添加 `@api` 别名。

```json
{
  "@api": "./api"
}
```
