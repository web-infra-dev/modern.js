import { app } from 'electron';
import Runtime from '@modern-js/electron-runtime';

const runtime = new Runtime({
  windowsConfig: [{
    name: 'main',
  }],
  mainServices: {
     test: () => 'hello, modern js electron'
  },
});

app.whenReady().then(async () => {
  await runtime.init();

  winService.createWindow({
    name: 'main'
  })

});
