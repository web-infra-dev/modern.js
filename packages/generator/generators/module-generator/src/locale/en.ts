export const EN_LOCALE = {
  get_packages_error:
    'get all packages failed，please check this environment and try again',
  success: `Success！
{packageManager} dev          # Run and debug the project
{packageManager} build        # Build the module for production
{packageManager} lint         # Run ESLint and automatically fix problems
{packageManager} change       # Add a new changeset
{packageManager} bump         # Update version and changelog via changeset
{packageManager} new          # Enable optional features
{packageManager} test         # Run test cases
{packageManager} release      # Release the package`,
};
