export const EN_LOCALE = {
  get_packages_error:
    'get all packages failed，please check this environment and try again',
  success: `Success！
You can run the following command in the directory of the new project:
pnpm dev          # Starting the dev server
pnpm build        # Build the app for production
pnpm serve        # Preview the production build locally
pnpm lint         # Run ESLint and automatically fix problems
pnpm new          # Enable optional features or add a new entry`,
};
