import { AppToolsUserConfig, LegacyAppToolsUserConfig } from './types';

export const defineConfig = (config: AppToolsUserConfig) => config;
export const defineLegacyConfig = (config: LegacyAppToolsUserConfig) => config;
