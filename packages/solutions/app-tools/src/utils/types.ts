export type DevOptions = {
  entry?: string[] | boolean;
  apiOnly?: boolean;
  analyze?: boolean;
};

export type BuildOptions = {
  analyze?: boolean;
};

export type StartOptions = {
  apiOnly?: boolean;
};
