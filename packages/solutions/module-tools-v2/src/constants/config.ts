import type { SourceConfig, ToolsConfig } from '../types';

export const defaultSource: SourceConfig = {
  envVars: [],
  globalVars: {},
  alias: {},
  designSystem: {},
};

export const defaultTools: ToolsConfig = {};
