# AGENTS.md — {{packageName}}

> 给 AI Agent（Claude Code / Cursor 等）的常驻指引。保持精简：只放命令、约束、版本、路由。

## 命令
- 安装：`pnpm install`
- 开发：`pnpm dev`（`modern dev`）
- 构建：`pnpm build`（`modern build`）
- 预览：`pnpm serve`
- 校验：`pnpm lint`

## 约束
- 包管理器：pnpm（不要混用 npm / yarn）。
- 禁止手改：`pnpm-lock.yaml`、`dist/`、`node_modules/`。
- 约定式路由在 `src/routes`，运行时入口 `src/modern.runtime.ts`。
- 改完代码必须跑：`pnpm lint` + `pnpm build`。

## 版本优先级
- 本项目使用 Modern.js v{{version}}。回答与改动须匹配该版本，不要使用更新版本未发布的特性。

## 查文档（知识检索）
- API / 配置 / 概念优先查 llms.txt：https://modernjs.dev/llms.txt
- 不要把整份文档复制进上下文，按需取片段。

## 复杂任务（走 Skills，按需选装）
- 升级到 v3：`modernjs-migrate-to-v3`
- 依赖体检：`modernjs-dependency-audit`
- Skills 默认不强装；安装方式见 https://modernjs.dev/guides/get-started/ai-coding-agents
