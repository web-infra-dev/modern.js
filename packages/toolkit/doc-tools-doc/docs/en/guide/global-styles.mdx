# Custom Global Styles

In some scenarios, you may need to add some global styles based on the theme UI. The framework provides a config param `globalStyles` to implement it.

## Usage

Add the following config to `modern.config.ts`:

```ts title="modern.config.ts"
import docTools, { defineConfig } from '@modern-js/doc-tools';
import path from 'path';

export default defineConfig({
  doc: {
    globalStyles: [path.join(__dirname, 'styles/index.css')],
  },
  plugins: [docTools()],
});
```

Then you can add the following code:

```css
/* styles/index.css */
:root {
  --modern-c-brand: #f00;
}
```

In this way, the framework will automatically collect all global styles and merge them into the final style file.

> If you want to know more about internal global styles, you can check [vars.css](https://github.com/modern-js-dev/modern.js/blob/main/packages/cli/doc-core/src/theme-default/styles/vars.css) and [doc.css](https://github.com/modern-js-dev/modern.js/blob/main/packages/cli/doc-core/src/theme-default/styles/doc.css)。
