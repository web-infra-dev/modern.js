export default function RemotePanel() {
  return (
    <section data-testid="remote-panel">
      <strong>Provider: billing widget</strong>
      <p>
        The remote exposes a component that reports a duplicated React source
        from a bundled dependency.
      </p>
    </section>
  );
}

