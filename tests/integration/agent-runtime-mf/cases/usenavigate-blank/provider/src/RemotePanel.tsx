export default function RemotePanel() {
  return (
    <section data-testid="remote-panel">
      <strong>Provider: lineage feature page</strong>
      <p>
        The remote exposes navigation state so the host can tell route mismatch
        from component mount failure.
      </p>
    </section>
  );
}

