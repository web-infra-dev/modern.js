import { applyBaseConfig } from '../../utils/applyBaseConfig';

export default applyBaseConfig({
  source: {
    mainEntryName: 'index',
  },
  html: {
    titleByEntries: {
      index: 'TikTok',
    },
  },
});
