// Server-safe expose wrapper for the client Counter component.
// Use an explicit import so the RSC server transform parses the module.
import Counter from '../components/Counter';
export default Counter;
